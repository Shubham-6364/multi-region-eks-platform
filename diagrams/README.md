# Diagrams Directory

Stores architectural diagrams (e.g., Mermaid, Draw.io, or images) representing the platform's topology.

## Interconnection
- Visually explains the AWS foundation from `terraform/`.
- Illustrates traffic flow, Route53 failover, and Istio service mesh behavior.
- Included and referenced within the documentation in `docs/` and the root `README.md`.
